const pi = 3.14; //here when const use always that variables will be constant we can't change in that code.
let account_id = "32344";
var noUse = "this time not use because choose only priority"
var noUse = "when call these same name var so only lsat wal a hi lega"
let account_status;//ek ye bhistyle hai use krne ka var its undefined

console.log(pi);
console.log(account_id);
console.log(noUse);

console.table([ pi, account_id, noUse,account_status]);